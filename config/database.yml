# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#

######this used to be the default before goku started playing around
# default: &default
#   adapter: sqlite3
#   pool: 5
#   timeout: 5000

default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: gokuadmin
  password: blahblah
  host: localhost
  socket: C:/xammp/mysql/mysql.sock


development:
  <<: *default
  database: testApp_development
  ####database: db/development.sqlite3 #used to be default

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3

production:
  <<: *default
  database: db/production.sqlite3
