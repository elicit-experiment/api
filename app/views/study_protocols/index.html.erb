<p id="notice"><%= notice %></p>

<h1>Study Protocols</h1>

<table>
  <thead>
    <tr>
      <th>Belongs to</th>
      <th>Belongs to</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @study_protocols.each do |study_protocol| %>
      <tr>
        <td><%= study_protocol.belongs_to %></td>
        <td><%= study_protocol.belongs_to %></td>
        <td><%= link_to 'Show', study_protocol %></td>
        <td><%= link_to 'Edit', edit_study_protocol_path(study_protocol) %></td>
        <td><%= link_to 'Destroy', study_protocol, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Study Protocol', new_study_protocol_path %>
